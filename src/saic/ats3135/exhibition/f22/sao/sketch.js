function setup() {
   //h = document.documentElement.scrollHeight;
  //createCanvas(windowWidth, h);
}

function draw() {
  //background(97, 55, 13);
}
function windowResized(){
    //createCanvas(windowWidth, h);
}